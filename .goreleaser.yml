project_name: axiom-loki-proxy

builds:
  - <<: &build_defaults
      binary: axiom-loki-proxy
      main: ./cmd/axiom-loki-proxy
      env:
        - CGO_ENABLED=0
      flags:
        - -buildmode=exe
        - -installsuffix=cgo
        - -trimpath
        - -tags="osusergo netgo static_build"
      ldflags:
        - -s -w -extldflags "-fno-PIC -static"
        - -X github.com/axiomhq/axiom-loki-proxy/version.release={{ .Version }}
        - -X github.com/axiomhq/axiom-loki-proxy/version.revision={{ .ShortCommit }}
        - -X github.com/axiomhq/axiom-loki-proxy/version.buildDate={{ .CommitDate }}
        - -X github.com/axiomhq/axiom-loki-proxy/version.buildUser=goreleaser
      mod_timestamp: "{{ .CommitTimestamp }}"
    id: darwin
    goos: 
      - darwin
    goarch: 
      - amd64

  - <<: *build_defaults
    id: linux
    goos: 
      - linux
    goarch: 
      - 386
      - amd64
      - arm64
    goarm: 
      - 6
      - 7

  - <<: *build_defaults
    id: windows
    goos: 
      - windows
    goarch: 
      - 386
      - amd64

archives:
  - <<: &archive_defaults
      name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}"
    id: nix
    builds:
      - darwin
      - linux
    wrap_in_directory: true
    format: tar.gz
    files:
      - LICENSE
      - README.md
  - <<: *archive_defaults
    id: windows
    builds:
      - windows
    wrap_in_directory: false
    format: zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

milestones:
  - repo:
      owner: axiomhq
      name: axiom-loki-proxy
    close: true
    fail_on_error: false

release:
  github:
    owner: axiomhq
    name: axiom-loki-proxy
  prerelease: auto
  name_template: "Axiom Loki Proxy v{{ .Version }}"

dockers:
  - image_templates:
      - "axiomhq/axiom-loki-proxy:{{ .Major }}"
      - "axiomhq/axiom-loki-proxy:{{ .Major }}.{{ .Minor }}"
      - "axiomhq/axiom-loki-proxy:{{ .Major }}.{{ .Minor }}.{{ .Patch }}"
      - "axiomhq/axiom-loki-proxy:latest"
